[project]
name = "zdisamar-tools"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
    "altair>=5.5",
    "numpy>=2.2",
    "pandas>=2.2",
    "vl-convert-python>=1.7",
]

[dependency-groups]
demo = [
    "ipykernel>=7.1",
    "matplotlib>=3.10",
    "nbclient>=0.10",
    "nbformat>=5.10",
]

[tool.uv]
package = false

# Forward-Model Tracing

This folder keeps retained trace summaries from earlier O2 A forward-model
performance investigations.

The source-specific trace capture scripts were removed with the Rust port
cleanup. Treat the JSON summaries under `output/` as historical evidence until a
new Rust-native tracing workflow is added.

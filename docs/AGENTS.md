# Docs

- `docs/` is for version-controlled documentation about repo conventions and workflows.
- `docs/specs/` is intentionally local-only scratch space and remains gitignored.
- `docs/workpackages/` is also local-only scratch space and remains gitignored.

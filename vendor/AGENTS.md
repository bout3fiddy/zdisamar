# Vendor

- `vendor/disamar-fortran/` is a local, gitignored clone of the upstream KNMI DISAMAR Fortran source.
- Refresh it with `./scripts/bootstrap-upstream.sh`.
- Do not commit files from the vendored clone unless the repo policy changes deliberately.

# Specs

- `specs/` contains tracked architecture and migration documents that should stay version-controlled.
- Keep these files stable and decision-oriented; local scratch notes belong under `docs/specs/`, which is gitignored.
- When implementation changes invalidate a tracked spec, update the spec in the same change.

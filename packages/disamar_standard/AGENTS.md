# disamar_standard

- This package is the home for the bundled DISAMAR 1D model family on the shared runtime.
- Preserve the scientific feature set while removing legacy application coupling.
- Legacy Fortran parity work should map against `vendor/disamar-fortran/` and the local scratch design notes under `docs/specs/` when present.

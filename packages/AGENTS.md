# Packages

- Packages represent distributable model families, mission bundles, or builtin feature sets layered on the shared runtime.
- Shared engine behavior belongs in `src/`; package directories should compose and configure that behavior rather than reimplement it.
- Put package-specific instructions in the nearest package `AGENTS.md`.
